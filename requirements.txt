# ============================================================================
# Fiducia DQMS — Python Dependencies (Unified Layout)
# ============================================================================

# -- API Framework --
# (FastAPI removed; services migrated to Django)
uvicorn[standard]>=0.29.0,<0.30.0
python-multipart>=0.0.9
requests>=2.31.0

# -- Django (for migration from FastAPI) --
Django>=4.2,<5.0
gunicorn>=22.0.0
drf-spectacular>=0.27.0

# -- Settings --
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.0

# -- Database Drivers --
# Automatically compiles native C-bindings on Linux/Docker setups, 
# while defaulting to lightweight wheels for simple local workstation tasks.
psycopg2>=2.9.9,<3.0.0; sys_platform == 'linux'
psycopg2-binary>=2.9.9,<3.0.0; sys_platform != 'linux'

# -- Redis / Celery --
redis>=5.0.0,<6.0.0
celery[redis]>=5.3.0,<6.0.0

# -- JWT --
PyJWT>=2.8.0,<3.0.0
cryptography>=42.0.0

# -- Logging --
structlog>=24.1.0

# -- HTTP client (for inter-service calls & testing clients) --
httpx>=0.27.0

# -- Templating (Notification Service) --
jinja2>=3.1.0

# -- Migrations --
alembic>=1.13.0

# -- Testing Frameworks --
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0

# -- Performance Testing --
locust>=2.29.0